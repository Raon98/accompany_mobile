export function fileRoute() {
    // const fs = require('fs');
    //
    // fs.readdir('./src', (err: NodeJS.ErrnoException | null, files: string[]) => {
    //     // 에러가 있다면 에러를 출력하고 함수를 종료합니다.
    //     if (err) {
    //         console.error('Error reading directory:', err);
    //         return;
    //     }
    //
    //     // 파일 목록을 출력합니다.
    //     console.log('Files in src directory:');
    //     files.forEach(file => {
    //         console.log(file);
    //     });
    // });
}
